#include <stdio.h>

int main() {
    double s, d, t;
    scanf("%lf", &s);
    scanf("%lf", &d);
    scanf("%lf", &t);

    double time_to_reach = (d * 3600) / s;

    if (time_to_reach < t) {
        printf("Yes\n");
    } else {
        printf("No\n");
    }

    return 0;
}
