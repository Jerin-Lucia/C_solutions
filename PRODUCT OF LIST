#include <stdio.h>

int main() {
    int n, i;
    float num, product = 1;

    scanf("%d", &n);
    for (i = 0; i < n; i++) {
        scanf("%f", &num);
        product *= num;
    }
    printf("The Product of list value is %.2f\n", product);

    return 0;
}
