#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int main() {
    int l,b,lp1,bp1,lp2,bp2;
    scanf("%d\n%d\n%d\n%d\n%d\n%d\n",&l,&b,&lp1,&bp1,&lp2,&bp2);
    int a=l*b;
    int a2=lp1*bp1;
    int a3=lp2*bp2;
    int a4=a2+a3;
    if (a>=a4){
        printf("Raj can fix both painting");
    }else{
        printf("Raj cannot fix both painting");
    }
    return 0;
}
